// components/Metrics.js
import styled from 'styled-components';

const Metrics = () => (
  <Section>
    <h2>Metrics</h2>
    <p>Show server and active user count here.</p>
  </Section>
);

export default Metrics;

const Section = styled.section`
  padding: 2rem;
  text-align: center;
`;
