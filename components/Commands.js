// components/Commands.js
import styled from 'styled-components';

const Commands = () => (
  <Section>
    <h2>Commands</h2>
    <p>Showcase the commands users can use with RhythmoSync.</p>
  </Section>
);

export default Commands;

const Section = styled.section`
  padding: 2rem;
  text-align: center;
`;
